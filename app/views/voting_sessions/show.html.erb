<h1>Voting Session Details</h1>
<h2>Special Offers:</h2>
<% if @special_offers %>
  <ul>
    <% @special_offers.each do |offer| %>
      <li><%= link_to offer.title, special_offer_path(offer) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No special offers available for this voting session.</p>
<% end %>
<h2>Friends in this Voting Session:</h2>
<% if @friends %>
  <ul>
    <% @friends.each do |friend| %>
      <li><%= friend.name %></li>
    <% end %>
  </ul>
<% else %>
  <p>No other friends in this voting session yet.</p>
<% end %>
