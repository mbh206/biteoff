<div class="container">
  <div class="my-3 p-1 d-flex flex-row justify-content-between align-items-center bg-light pt-2 rounded shadow-sm">
    <div>
      <%= cl_image_tag current_user.photo.key, alt: "avatar image", class: "avatar-large" %>
    </div>
    <div>
      <span class="h5">Hello <%= @user.username %>!</span>
    </div>
    <div class="text-center">
      <% if user_signed_in? %>
        <div class="btn btn-info">
          <%= link_to destroy_user_session_path, data: { turbo_method: :delete } do %>
            <i class="fa-solid fa-door-open"></i>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
  <%=render "friends_list"%>
</div>
